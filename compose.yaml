services:
  sakay-client:
    build:
      context: ./Client
      dockerfile: Dockerfile
    container_name: sakay-client
    restart: unless-stopped
    ports:
      - "8181:80"
    environment:
      - NODE_ENV=production
    networks:
      - sakay-network

networks:
  sakay-network:
    driver: bridge
